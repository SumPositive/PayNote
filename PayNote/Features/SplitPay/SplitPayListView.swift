import SwiftUI
import SwiftData

struct SplitPayListView: View {
    var record: E3record? = nil

    var body: some View {
        Text("splitPay.list.stub")
            .navigationTitle("splitPay.list.title")
            .navigationBarTitleDisplayMode(.large)
    }
}
