import SwiftUI
import SwiftData

struct BankEditView: View {
    var body: some View {
        Text("bank.edit.stub")
            .navigationTitle("bank.edit.title.add")
            .navigationBarTitleDisplayMode(.inline)
    }
}
