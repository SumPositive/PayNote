import SwiftUI
import SwiftData

struct BankListView: View {
    var body: some View {
        Text("bank.list.stub")
            .navigationTitle("top.bankList")
            .navigationBarTitleDisplayMode(.large)
    }
}
