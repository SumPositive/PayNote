import SwiftUI
import SwiftData

struct CardEditView: View {
    var body: some View {
        Text("card.edit.stub")
            .navigationTitle("card.edit.title.add")
            .navigationBarTitleDisplayMode(.inline)
    }
}
