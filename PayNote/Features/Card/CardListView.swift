import SwiftUI
import SwiftData

struct CardListView: View {
    var body: some View {
        Text("card.list.stub")
            .navigationTitle("top.cardList")
            .navigationBarTitleDisplayMode(.large)
    }
}
