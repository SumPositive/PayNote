import SwiftUI
import SwiftData

struct PaymentListView: View {
    var body: some View {
        Text("payment.list.stub")
            .navigationTitle("top.paymentList")
            .navigationBarTitleDisplayMode(.large)
    }
}
