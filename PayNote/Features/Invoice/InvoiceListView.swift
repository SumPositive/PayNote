import SwiftUI
import SwiftData

struct InvoiceListView: View {
    var card: E1card? = nil

    var body: some View {
        Text("invoice.list.stub")
            .navigationTitle("invoice.list.title")
            .navigationBarTitleDisplayMode(.large)
    }
}
