import SwiftUI
import SwiftData

struct CategoryListView: View {
    var body: some View {
        Text("category.list.stub")
            .navigationTitle("top.categoryList")
            .navigationBarTitleDisplayMode(.large)
    }
}
