import SwiftUI
import SwiftData

struct CategoryEditView: View {
    var body: some View {
        Text("category.edit.stub")
            .navigationTitle("category.edit.title.add")
            .navigationBarTitleDisplayMode(.inline)
    }
}
