import SwiftUI
import SwiftData

struct RecordListView: View {
    var body: some View {
        Text("record.list.stub")
            .navigationTitle("top.recordList")
            .navigationBarTitleDisplayMode(.large)
    }
}
