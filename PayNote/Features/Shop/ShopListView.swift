import SwiftUI
import SwiftData

struct ShopListView: View {
    var body: some View {
        Text("shop.list.stub")
            .navigationTitle("top.shopList")
            .navigationBarTitleDisplayMode(.large)
    }
}
