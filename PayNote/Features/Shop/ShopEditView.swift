import SwiftUI
import SwiftData

struct ShopEditView: View {
    var body: some View {
        Text("shop.edit.stub")
            .navigationTitle("shop.edit.title.add")
            .navigationBarTitleDisplayMode(.inline)
    }
}
