name: CreditMemo
options:
  bundleIdPrefix: com.azukid
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true
  generateEmptyDirectories: false
  xcodeVersion: "16"

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    DEVELOPMENT_TEAM: 5C2UYK6F45
    TARGETED_DEVICE_FAMILY: "1,2"
    IPHONEOS_DEPLOYMENT_TARGET: "18.0"
    ENABLE_USER_SCRIPT_SANDBOXING: YES

targets:
  CreditMemo:
    type: application
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - path: CreditMemo
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.azukid.AzCreditS1
        MARKETING_VERSION: "1.3.0"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_FILE: CreditMemo/Resources/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
      configs:
        Debug:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
        Release:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: ""

  CreditMemoTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: CreditMemoTests
    settings:
      base:
        BUNDLE_LOADER: "$(TEST_HOST)"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/CreditMemo.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/CreditMemo"
    dependencies:
      - target: CreditMemo

  CreditMemoUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: CreditMemoUITests
    dependencies:
      - target: CreditMemo
